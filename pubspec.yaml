name: flutter_project_sap
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- UI & Utilities ---
  cupertino_icons: ^1.0.6
  lottie: ^3.1.0
  intl: ^0.19.0         # Versi stabil terbaru
  
  # --- Functional ---
  http: ^1.2.0
  image_picker: ^1.0.7
  
  # 🔥 UPDATE KE VERSI INI BIAR ERROR WINDOWS HILANG 🔥
  file_picker: ^8.0.0   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true